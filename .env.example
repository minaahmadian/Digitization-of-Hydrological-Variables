# Copy this file to ".env" in the project root (same folder as .gitignore).
# The real ".env" is gitignored and will not be pushed to GitHub.
#
# Gemini (Google AI Studio): https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: model id (default in notebook if unset)
# GEMINI_MODEL=gemini-2.0-flash
